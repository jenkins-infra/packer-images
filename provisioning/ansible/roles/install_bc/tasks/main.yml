---
# Replaces the install_bc() function of provisioning/ubuntu-provision.sh.
- name: Install bc
  ansible.builtin.apt:
    name: bc
    state: present
    install_recommends: false
    update_cache: true
