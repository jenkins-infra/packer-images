---
# Replaces the install_JA_requirements() function of provisioning/ubuntu-provision.sh.
- name: Install the Jenkins agent common requirements
  ansible.builtin.apt:
    name:
      - make
      - unzip
      - zip
      - parallel
    state: present
    install_recommends: false
    update_cache: true
